import {StyleSheet} from "react-native";

const styles = StyleSheet.create({
  flatListContainer: {
    flexGrow: 0,
    paddingTop: 20,
    paddingBottom: 40,
  },
  postContent: {
    paddingHorizontal: 8,
    marginBottom: 12,
  },
});

export default styles;
